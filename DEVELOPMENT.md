# LearnTrack 开发说明书

## 1. 文档目的

LearnTrack 是一个面向个人学习场景的本地优先 Web 应用。它解决的核心问题是：用户不必进入强制专注模式，也能自由记录真实学习时间，并按科目、活动、日期和学习路线查看统计与进度。

本说明书定义首版产品边界、系统结构、核心数据口径、同步与备份策略、开发顺序和验收标准。后续实现应以此文档为基线；若产品规则发生变化，应先更新文档和相关数据约束，再修改代码。

当前仓库只建立目录和本文档，尚未创建任何业务源码、配置文件、数据库文件或部署文件。

## 2. 产品目标

首版服务单个用户，支持电脑和手机浏览器访问，并可以安装为 PWA。应用在断网时仍可新增、编辑和查看本机已有数据；联网后与部署在 ECS 或本机上的同步服务交换数据。

首版必须完成以下闭环：

1. 用户通过手动时长、起止时间或实时计时创建学习记录。
2. 每条记录归属“大科目 → 具体科目 → 学习活动”。
3. 用户可以查看时间总览、趋势、科目分布、小时分布和日历热力图。
4. 用户主动创建学习路线后，可以按章节或数量记录进度。
5. 一次学习记录可以选择同时更新路线进度，两类数据仍保持独立可修正。
6. 用户可以创建轻量待办以及每日、每周学习目标。
7. 数据可以在个人设备间同步，并能完整导出、备份和迁移。

首版不包含公开注册、多人协作、社交排行、付费、自动生成课程目录、在线抓取题库、复杂排课、间隔复习算法和原生移动应用。

## 3. 技术基线

建议采用以下技术栈，开始编码前固定实际版本并提交依赖锁文件：

| 层 | 技术 | 职责 |
| --- | --- | --- |
| Web | React、TypeScript、Vite | 页面、交互和 PWA 客户端 |
| UI | Tailwind CSS、Radix UI | 设计令牌和无障碍基础组件 |
| 图表 | ECharts | 趋势、分布、热力图和下钻交互 |
| 本地数据 | IndexedDB、Dexie | 离线数据、事务和待同步操作队列 |
| API | Node.js 24 LTS、Fastify | 登录、同步、备份、资源和健康检查 |
| 服务端数据 | SQLite、better-sqlite3 | 个人工作区的服务端权威副本 |
| 校验 | Zod | API 契约、导入内容和配置校验 |
| 测试 | Vitest、Playwright | 领域规则、同步行为和端到端验收 |
| 部署 | Docker Compose、Nginx | ECS 部署、HTTPS 反向代理和持久卷 |

客户端和服务端共用 TypeScript 数据契约与纯领域计算。页面组件不得自行实现另一套统计公式；API 也不得通过名称关联业务对象。

## 4. 仓库目录

```text
LearnTrack/
├── DEVELOPMENT.md
├── apps/
│   ├── web/
│   │   ├── public/
│   │   └── src/
│   │       ├── app/
│   │       ├── components/
│   │       │   ├── common/
│   │       │   └── layout/
│   │       ├── features/
│   │       │   ├── auth/
│   │       │   ├── timer/
│   │       │   ├── entries/
│   │       │   ├── dashboard/
│   │       │   ├── analytics/
│   │       │   ├── subjects/
│   │       │   ├── learning-paths/
│   │       │   ├── todos/
│   │       │   ├── goals/
│   │       │   ├── settings/
│   │       │   ├── sync/
│   │       │   └── backup/
│   │       ├── routes/
│   │       ├── db/
│   │       ├── services/
│   │       ├── stores/
│   │       ├── styles/
│   │       ├── types/
│   │       ├── utils/
│   │       └── workers/
│   └── api/
│       └── src/
│           ├── app/
│           ├── modules/
│           │   ├── auth/
│           │   ├── sync/
│           │   ├── entries/
│           │   ├── subjects/
│           │   ├── learning-paths/
│           │   ├── todos/
│           │   ├── goals/
│           │   ├── assets/
│           │   ├── backups/
│           │   └── health/
│           ├── db/
│           ├── middleware/
│           ├── services/
│           └── utils/
├── packages/
│   ├── domain/src/
│   ├── contracts/src/
│   └── config/
├── database/
│   ├── migrations/
│   └── seeds/
├── infrastructure/
│   ├── docker/
│   ├── nginx/
│   └── systemd/
├── scripts/
│   ├── backup/
│   ├── restore/
│   └── migrate/
├── docs/
│   ├── architecture/
│   ├── product/
│   ├── api/
│   ├── operations/
│   └── testing/
├── tests/
│   ├── e2e/
│   └── fixtures/
└── var/
    ├── data/
    ├── backups/
    └── uploads/
```

目录职责如下：

- `apps/web` 只负责浏览器体验、本地数据库访问和同步调度。
- `apps/api` 只负责服务端能力，不包含页面组件。
- `packages/domain` 保存不依赖 React、Fastify 或数据库驱动的领域模型和计算函数。
- `packages/contracts` 保存 API 请求、响应、同步操作和备份格式的共享类型与校验规则。
- `packages/config` 保存可共享的 TypeScript、测试和代码质量配置。
- `database` 保存服务端数据库迁移与初始内置科目数据。
- `infrastructure` 保存部署模板；不得保存密钥、真实域名或生产数据。
- `scripts` 保存可独立运行的备份、恢复和迁移工具。
- `docs` 保存随实现补充的架构决策、接口说明、运维手册和测试记录。
- `tests/e2e` 覆盖跨页面与多客户端场景；`tests/fixtures` 只放脱敏、可重复生成的测试数据。
- `var` 是本地运行数据目录，后续必须加入忽略规则，不得提交数据库、备份、背景图片或凭据。

业务模块优先按功能聚合。一个功能内部可拥有自己的组件、hooks、查询、命令和测试，只有真正跨功能复用的内容才放入公共目录。

## 5. 页面与交互结构

### 5.1 总览

首屏应直接支持开始记录，不使用营销式欢迎页。内容按优先级排列：

1. 当前计时状态与开始、暂停、结束操作。
2. 常用活动快捷入口，如“高数·听课”“高数·习题”“英语·单词”“LeetCode·刷题”。
3. 今日与本周记录时长、目标完成度。
4. 今日待办和逾期未完成项。
5. 已由用户创建的学习路线进度。
6. 最近趋势和快速进入完整统计的入口。

### 5.2 记录

支持按天查看记录，提供三种录入方式：

- 仅填写日期和持续时长。
- 填写开始、结束时间，由系统计算时长。
- 使用实时正计时或可选倒计时生成记录。

记录列表支持编辑、删除、复制、按分类筛选和文本搜索。删除带有关联进度变更的记录时，询问是否同时撤销该次进度；默认只删除时间记录。

### 5.3 统计

支持日、周、月、年和自定义范围，至少提供：

- 总时长、记录次数、自然日日均和有记录日日均。
- 大科目、具体科目和活动三级分布。
- 每日、每周或每月趋势。
- 学习日历热力图。
- 按小时拆分的时间分布。
- 时间段统计覆盖率。
- 可选学习状态和打断原因统计。

图表应允许点击日期或分类查看原始记录，避免只提供无法核验的汇总数字。

### 5.4 学习

包括学习路线、轻量待办和时长目标。只有用户主动创建路线后才显示进度。

路线支持两种互斥模式：

- 章节模式：章节和小节最多两层，按叶子项目完成数计算进度。
- 数量模式：记录总量、完成量和单位，例如“1000 题，已完成 120 题”。

支持逐条新增章节和按行批量粘贴，不自动提供课程或题库内容。

### 5.5 设置

包括分类与快捷项管理、主题与背景、设备和同步状态、数据导出、备份恢复及个人账号设置。

桌面采用侧边导航，手机采用底部主导航。主要文字不得小于 14px，正文默认不小于 16px；自定义背景必须经过遮罩和对比度处理。

## 6. 分类规则

采用“大科目 → 具体科目 → 学习活动”三级结构。初始内置内容如下：

| 大科目 | 具体科目 | 活动示例 |
| --- | --- | --- |
| 数学 | 高数、线性代数、概率论与数理统计 | 听课、习题、复习 |
| 英语 | 单词、阅读 | 学习、练习、复习 |
| 408 | 数据结构、计算机组成原理、操作系统、计算机网络 | 听课、习题、复习 |
| 政治 | 综合学习 | 听课、习题、背诵、复习 |
| 算法 | LeetCode | 刷题、复盘 |

所有对象使用稳定 UUID，内置分类另有稳定种子标识。改名不改变关联关系。已有记录引用的分类只能归档，不能直接物理删除。自定义具体科目必须选择大科目，自定义活动必须选择具体科目。

## 7. 时间数据规则

### 7.1 时间记录

每条记录至少包含：记录 ID、设备 ID、所属活动 ID、记录方式、学习日期、有效时长、创建时间、修改时间、版本和删除状态。

根据记录方式附加以下字段：

- 仅时长记录：保存用户选择的本地学习日期，不伪造开始和结束时间。
- 时间段记录：保存 UTC 开始和结束时间，以及记录时采用的时区。
- 计时记录：额外保存计时会话来源、暂停区间和可选倒计时目标。

备注、学习状态评分、打断原因、关联路线和进度变更均为可选。

### 7.2 计时器

- 同一工作区正常情况下只有一个活动计时会话。
- 暂停期间不计入有效时长。
- 倒计时结束只提示，不自动停止；用户主动停止时保存全部实际时长。
- 页面刷新、后台运行或锁屏后，通过绝对时间戳和暂停区间恢复，不能依赖逐秒累加。
- 连续计时超过 12 小时时提示核对，但不得自动丢弃。
- 切换学习活动时，在切换时刻结束旧记录并开始新记录。

### 7.3 重叠记录

只有具备开始和结束时间的记录参与重叠检查。发现重叠时必须提示，用户仍可确认保存。确认后的每条记录均完整计入“记录时长”，产品内要明确这不是去重后的客观时钟时长。

仅填写日期和时长的记录不能判断是否重叠，也不能人为分配到一天中的某个小时。

## 8. 统计口径

统计仅使用已保存、未删除的正式记录。正在运行的计时可在界面上临时显示，但停止保存前不进入持久化汇总。

- 总时长：范围内各条记录有效时长之和。
- 记录次数：一条正式记录算一次，暂停和继续不增加次数。
- 自然日日均：总时长除以所选范围包含的自然日数。
- 有记录日日均：总时长除以至少存在一条记录的日期数。
- 分类占比：分类时长除以总时长；用户确认保留的重叠记录照常累计。
- 趋势和热力图：按本地日期归属；跨午夜的时间段按当地午夜拆分。
- 小时分布：仅按具有实际时间段的记录拆分到小时；仅时长记录显示为“未指定时段”。
- 时间段覆盖率：具有实际时间段的有效时长除以总时长。
- 目标完成度：实际时长除以目标时长，完成超过 100% 时保留真实数值。

默认时区为 `Asia/Shanghai`，一周从周一开始。内部统一按整数秒保存和聚合，展示层再格式化为小时和分钟，避免逐条取整误差。

学习状态为 1 至 5 分的可选自评。未评分不等于零分，未填写打断不等于没有打断。单个小时少于 5 条有效评分时，不生成“最佳学习时段”结论。

## 9. 学习路线和进度

一条路线归属一个具体科目，并固定使用章节模式或数量模式。

章节模式的父章节进度由叶子小节计算；没有小节的章节自身作为叶子项。数量模式保存总量、完成量和单位。完成量不能为负数；超过总量时要求用户确认或调整总量，不静默截断。

学习记录可以附带一次进度更新：

- 章节模式可完成或取消完成一个项目。
- 数量模式可增加或减少一个数量。
- 保存动作在本地事务中同时写入时间记录和进度变更。
- 修改时间记录的时长不自动修改进度。
- 删除记录时默认保留进度，也允许撤销与该记录直接关联的进度变更。
- 每次进度变化都以独立事件保存，以便同步去重、审计和精确撤销。

没有路线的科目只展示时间统计，不显示空进度或推测进度。不同单位的路线不计算一个混合总百分比。

## 10. 待办和目标

待办包含标题、可选科目、可选路线或章节、安排日期、可选截止时间及完成状态。完成待办不自动增加时长，也不自动改变章节进度。

目标支持以下维度：

- 全部学习的每日或每周时长。
- 某个具体科目的每日或每周时长。

同一范围、同一周期只保留一个有效目标。所有来源的正式学习记录均计入目标。

## 11. 本地优先与同步设计

### 11.1 数据流

所有写入遵循固定顺序：

```text
用户操作 → IndexedDB 本地事务 → 立即更新界面 → 写入待同步队列
         → 网络可用时推送 ECS → 服务端幂等确认 → 其他设备增量拉取
```

任何日常保存操作都不得等待网络。首次登录并完成本机初始化后，会话过期或服务不可达不能阻止用户继续离线记账。

### 11.2 同步原则

- 每个客户端操作拥有唯一操作 ID，服务端重复接收时只应用一次。
- 每个业务对象包含版本，修改操作携带客户端看到的基础版本。
- 同一设备产生的连续操作按本地顺序提交。
- 不同对象的修改直接合并。
- 同一对象被多设备并发编辑时保留双方候选，由用户选择，不静默覆盖。
- 数量进度使用独立增量事件按操作 ID 去重。
- 删除使用删除标记同步，避免旧设备恢复已删除对象。
- 冲突候选不重复进入正式统计。
- 重新联网、应用回到前台或用户主动刷新时立即同步；前台在线期间按固定短间隔拉取。

离线状态下可能在两台设备同时开始计时。联网后不得删除任一会话，应提示用户选择保留、调整或合并为两条记录。

### 11.3 API 边界

首版统一使用 `/api/v1`，规划以下接口组：

| 接口组 | 责任 |
| --- | --- |
| `auth` | 单用户登录、登出和会话读取 |
| `sync/bootstrap` | 初始化工作区和同步游标 |
| `sync/push` | 接收操作、事务组和基础版本 |
| `sync/pull` | 按游标返回增量修改与删除标记 |
| `assets` | 上传和下载背景资源 |
| `backups` | 生成、下载、校验和恢复备份 |
| `health` | 服务、数据库和版本健康检查 |

公开注册默认关闭。公网部署使用 HTTPS、HttpOnly 会话 Cookie、密码哈希和登录限流。备份脚本使用只能读取备份的独立凭据。

## 12. 备份、恢复与迁移

完整备份采用带格式版本的 ZIP 包：

```text
backup.zip
├── manifest.json
├── data.json
└── assets/
```

`manifest.json` 保存格式版本、生成时间、工作区标识、对象数量和校验信息；`data.json` 保存业务数据、未同步操作及冲突恢复所需信息；`assets` 保存用户背景。登录会话和访问凭据不得进入业务备份。

另提供 CSV 时间明细导出供人工分析，但 CSV 不承担完整恢复职责。

服务端每天生成一致性数据库快照，默认保留 7 个每日版本和 4 个每周版本。macOS 备份脚本在登录时及每 6 小时尝试将新备份下载到 `/Users/paoooo/Documents/StudyTrackerBackups`，下载后校验完整性，成功后才记录保存时间。设置页分别显示服务器生成时间和电脑确认保存时间。

恢复流程必须先校验格式、版本、文件哈希和数据约束，再显示对象数量与时间范围摘要。用户确认恢复后，先备份现有环境，再原子替换数据。恢复失败不得改变现有数据。

ECS 迁移优先保持原域名：旧服务进入只读、生成最终备份、新环境恢复并校验、切换域名解析、完成跨设备同步验证，最后停用旧服务。若旧 ECS 已不可用，则从电脑备份恢复并生成新的同步代号，旧设备重新对账，不能用旧同步游标覆盖恢复环境。

## 13. 外观和资源约束

默认界面为简洁浅色学习仪表盘，同时支持深色和跟随系统。科目颜色在快捷入口、记录和图表中保持一致。

用户背景支持 JPG、PNG 和 WebP。原始文件上限 10 MB，浏览器导入后应缩放压缩到 2 MB 以内，并提供位置、模糊和遮罩设置。资源按内容哈希保存，避免多设备重复上传相同文件。

Service Worker 只缓存应用外壳和必要静态资源。业务数据以 IndexedDB 为准，背景资源有本地副本和服务端副本。版本更新采用提示刷新，刷新前必须保存表单草稿和计时状态。

## 14. 配置与秘密管理

实现阶段应提供脱敏配置示例，但绝不提交真实值。配置至少覆盖：

- Web 与 API 公开地址。
- 服务监听地址与端口。
- SQLite 数据文件和资源目录。
- 会话签名密钥与初始个人账号。
- Cookie 安全选项和可信代理配置。
- 备份保留周期、生成目录和下载凭据。
- 上传大小限制、日志级别和时区默认值。

所有生产秘密通过 ECS 环境或受限文件注入。日志不得输出密码、会话 Cookie、备份令牌或完整备份内容。

## 15. 开发约定

- 文件和代码标识使用英文，界面文字首版使用简体中文。
- 时间变量必须带清晰单位，例如 `durationSeconds`；禁止无单位的 `duration`。
- 绝对时间使用 ISO 8601 或整数时间戳传输，学习日期使用 `YYYY-MM-DD`。
- API 契约先在 `packages/contracts` 定义并校验，再分别接入客户端和服务端。
- 领域函数保持纯函数，对相同输入必须产生相同输出。
- 数据库结构变化只能通过追加迁移完成，已发布迁移不得回写修改。
- 不允许页面直接拼接 SQL，也不允许 API 返回数据库行作为未定义的公共格式。
- 错误信息向用户说明影响和可执行操作；详细堆栈只写入服务端日志。
- 无障碍要求包括键盘操作、清晰焦点、语义标签、足够对比度及减少动画偏好。

## 16. 实施阶段

### 阶段一：纯本地记录

建立工程配置、共享模型、IndexedDB 迁移、三级分类、快捷补录、起止时间和实时计时。完成基础记录列表、编辑删除和 JSON 完整导出恢复。

通过条件：断网、刷新和关闭后重新打开均不会丢失记录；暂停、跨午夜和倒计时超时计算正确。

### 阶段二：学习和统计

实现总览、全部统计、学习路线、进度事件、待办、目标、主题和背景。

通过条件：图表能下钻到原始记录；时间记录与进度更新可以共同保存并分别修正；没有目录的科目不显示进度。

### 阶段三：服务端和多端同步

实现个人登录、SQLite 迁移、同步推拉、幂等操作、冲突处理、资源同步和设备状态。

通过条件：两个独立浏览器在断网时修改数据，恢复联网后无重复、无静默覆盖、无删除对象复活。

### 阶段四：部署和迁移

完成 ECS Docker Compose、Nginx 配置、本机直接启动方式、备份脚本、恢复工具和运维说明。

通过条件：在一个全新目录中从电脑备份恢复服务，关键对象数量、总时长、路线进度和背景资源均一致。

## 17. 测试和验收清单

领域测试至少覆盖：

- 仅时长记录、时间段记录、跨午夜拆分和暂停区间计算。
- 用户确认的重叠记录完整累计。
- 日、周、月边界和 `Asia/Shanghai` 时区。
- 分类三级聚合、日均、覆盖率和目标完成度。
- 章节叶子进度、数量增减、关联撤销和重复操作去重。
- 同步版本冲突、删除标记、丢失回执后的重试。
- 备份格式校验、损坏文件拒绝和旧版本迁移。

端到端测试至少覆盖：

1. 补记昨天高数习题 90 分钟，只进入“未指定时段”。
2. 23:30 至次日 00:30 在两天各统计 30 分钟。
3. 学习 20 分钟、暂停 10 分钟、继续 15 分钟，最终保存 35 分钟。
4. 25 分钟倒计时在第 40 分钟停止，保存实际 40 分钟。
5. 记录 90 分钟并增加 20 题，网络重试不重复增加题数。
6. 删除上述记录时，默认保留进度；选择一起撤销时只撤销关联变更。
7. 没有学习路线时仍可完整记时和统计。
8. 离线创建记录、关闭应用、重新打开和联网同步。
9. 两个设备并发编辑同一记录时出现冲突选择界面。
10. 从完整备份恢复后，对象数量、汇总时长和路线进度一致。

目标验证数据量为 5 万条时间记录。常用范围的本地统计在桌面设备上目标响应时间为 1 秒以内；保存操作以本地写入完成为成功，不受 ECS 网络延迟影响。

## 18. 首版完成定义

满足以下条件才视为首版完成：

- 用户无需进入专注模式即可通过三种方式记录学习时间。
- 手机和电脑在离线、联网切换后能够使用同一份数据。
- 所有统计口径与原始记录一致并可追溯。
- 用户添加目录后才能看到相应进度，时间与进度不会被错误绑定。
- ECS 到期或失效后，可以通过电脑备份迁移到新 ECS 或本机。
- 自动化检查通过，并完成一次真实的备份恢复演练。

